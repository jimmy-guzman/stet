[install]
# Flat node_modules. Bun defaults a workspace root to the isolated linker,
# whose symlinked .bun store breaks Next.js/Turbopack's RSC client manifest
# resolution (global-error builtin) in dev. Hoisted matches what the TUI
# already used before the workspaces existed.
linker = "hoisted"
