/** Rows the references overlay shows before its windowed list scrolls. */
export const REFERENCES_MAX_ROWS = 14;
/** Rows the symbol outline overlay shows before its windowed list scrolls. */
export const SYMBOLS_MAX_ROWS = 14;
